module.exports = {
  // baseUrl:'./', 弃用
  outputDir: 'dist',
  publicPath: process.env.NODE_ENV === 'production' ? './' : '/' ,
  // publicPath: process.env.NODE_ENV === 'production' ? '/vant-demo/' : '/' ,


};
